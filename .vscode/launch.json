{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "run",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/code/target/debug/code",
            "cwd": "${workspaceFolder}/code",
            "preLaunchTask": "build"
        }
    ]
}
